# Sourcegraph Cody API Configuration
# Copy this file to .env and fill in your actual values

# Your Sourcegraph instance URL (required)
SOURCEGRAPH_URL=https://sourcegraph.com

# Your Sourcegraph access token (required)
# Get this from your Sourcegraph instance: Settings > Access tokens
SOURCEGRAPH_ACCESS_TOKEN=sgp_your_token_here

# Application identifier for API requests (required)
# This helps Sourcegraph identify your application in logs
SOURCEGRAPH_X_REQUESTED_WITH=cody-cookbook v1

# Optional: Default model to use across scripts
# Run 00-models.py to see available models in your instance
DEFAULT_MODEL=anthropic::2024-10-22::claude-sonnet-4-latest

# Optional: Default temperature for chat completions (0.0-1.0)
# Lower values = more focused, higher values = more creative
DEFAULT_TEMPERATURE=0.7

# Optional: Default maximum tokens for responses
# Higher values = longer responses but more costly
DEFAULT_MAX_TOKENS=4000
